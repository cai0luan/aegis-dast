node_modules/
build/
dist/
coverage/
# Âncora em "/" de propósito: sem ela, "data/" também casaria com src/data/ (que
# é código-fonte de verdade, não estado de runtime) — foi exatamente esse bug
# que quebrou o build da Vercel por excluir src/data/mockSecurityData.ts do git
# desde o primeiro commit deste repositório.
/data/
.DS_Store
*.log
.env*
!.env.example
worker/.venv/
worker/__pycache__/
# Binário de terceiros que cada operador instala localmente (ver worker/README.md)
# — nunca pertence a este repositório, licença própria e específico de plataforma.
worker/nuclei
worker/nuclei.exe

